<div
  class="fixed top-0 left-0 w-full h-screen pointer-events-none -z-10 overflow-hidden opacity-100 transition-opacity duration-500 ease-out cyberpunk-background"
>
  <!-- Capa de fondo más lenta (parallax layer 1) -->
  <div class="absolute top-0 left-0 w-full h-full parallax-layer parallax-back">
    <!-- Efectos de neón principales -->
    <div class="absolute rounded-full blur-[40px] animate-pulse-glow glow-1">
    </div>
    <div class="absolute rounded-full blur-[40px] animate-pulse-glow glow-2">
    </div>
    <div class="absolute rounded-full blur-[40px] animate-pulse-glow glow-3">
    </div>
    <div class="absolute rounded-full blur-[40px] animate-pulse-glow glow-4">
    </div>

    <!-- Grid cyberpunk -->
    <div
      class="cyber-grid absolute top-0 left-0 w-full h-full animate-grid-pulse"
    >
    </div>
  </div>

  <!-- Capa media (parallax layer 2) -->
  <div class="absolute top-0 left-0 w-full h-full parallax-layer parallax-mid">
    <!-- Líneas de código flotantes -->
    <div
      class="fixed font-mono text-sm opacity-0 -z-[5] animate-code-float line-1"
    >
      <span class="text-cyan-400 whitespace-nowrap code-text-glow-cyan"
        >const developer = new Creative();</span
      >
    </div>
    <div
      class="fixed font-mono text-sm md:text-xs lg:text-sm opacity-0 -z-[5] animate-code-float line-2"
    >
      <span class="text-fuchsia-500 whitespace-nowrap code-text-glow-magenta"
        >while(coding) &#123; dreams++; &#125;</span
      >
    </div>
    <div
      class="fixed font-mono text-sm md:text-xs lg:text-sm opacity-0 -z-[5] animate-code-float line-3"
    >
      <span class="text-indigo-500 whitespace-nowrap code-text-glow-indigo"
        >function buildFuture() &#123;</span
      >
    </div>
  </div>

  <!-- Capa delantera (parallax layer 3) -->
  <div
    class="absolute top-0 left-0 w-full h-full parallax-layer parallax-front"
  >
    <!-- Efectos de partículas -->
    <div
      class="absolute w-[3px] h-[3px] bg-cyan-400 rounded-full particle-shadow-cyan animate-particle-float particle-1"
    >
    </div>
    <div
      class="absolute w-[3px] h-[3px] bg-fuchsia-500 rounded-full particle-shadow-magenta animate-particle-float particle-2"
    >
    </div>
    <div
      class="absolute w-[3px] h-[3px] bg-indigo-500 rounded-full particle-shadow-indigo animate-particle-float particle-3"
    >
    </div>
    <div
      class="absolute w-[3px] h-[3px] bg-cyan-400 rounded-full particle-shadow-cyan animate-particle-float particle-4"
    >
    </div>
    <div
      class="absolute w-[3px] h-[3px] bg-purple-500 rounded-full particle-shadow-purple animate-particle-float particle-5"
    >
    </div>
    <div
      class="absolute w-[3px] h-[3px] bg-orange-500 rounded-full particle-shadow-orange animate-particle-float particle-6"
    >
    </div>
  </div>
</div>

<style>
  /* Animaciones que no se pueden hacer con Tailwind */
  @keyframes pulse-glow {
    0% {
      transform: scale(1);
      opacity: 0.3;
    }
    100% {
      transform: scale(1.1);
      opacity: 0.6;
    }
  }

  @keyframes code-float {
    0% {
      opacity: 0;
      transform: translateY(10px);
    }
    10% {
      opacity: 1;
    }
    90% {
      opacity: 1;
    }
    100% {
      opacity: 0;
      transform: translateY(-10px);
    }
  }

  @keyframes particle-float {
    0% {
      transform: translateY(0) translateX(0);
      opacity: 0;
    }
    20% {
      opacity: 1;
    }
    80% {
      opacity: 1;
    }
    100% {
      transform: translateY(-100px) translateX(50px);
      opacity: 0;
    }
  }

  @keyframes grid-pulse {
    0% {
      opacity: 0.2;
    }
    100% {
      opacity: 0.4;
    }
  }

  /* Clases personalizadas para efectos específicos */
  .cyberpunk-background.hidden {
    opacity: 0;
    visibility: hidden;
  }
  .parallax-layer {
    will-change: transform;
  }
  .parallax-back {
    transform: translateZ(-3px) scale(1.3);
  }
  .parallax-mid {
    transform: translateZ(-2px) scale(1.2);
  }
  .parallax-front {
    transform: translateZ(-1px) scale(1.1);
  }

  .animate-pulse-glow {
    animation: pulse-glow 3s ease-in-out infinite alternate;
  }
  .animate-code-float {
    animation: code-float 8s linear infinite;
  }
  .animate-particle-float {
    animation: particle-float 6s linear infinite;
  }
  .animate-grid-pulse {
    animation: grid-pulse 3s ease-in-out infinite alternate;
  }

  /* Efectos de resplandor neón específicos */
  .glow-1 {
    width: 400px;
    height: 400px;
    background: radial-gradient(circle, #00ffff 0%, transparent 70%);
    top: 10%;
    left: -10%;
    animation-duration: 4s;
    opacity: 0.4;
  }

  .glow-2 {
    width: 300px;
    height: 300px;
    background: radial-gradient(circle, #ff00ff 0%, transparent 70%);
    top: 60%;
    right: -5%;
    animation-duration: 5s;
    animation-delay: -1s;
    opacity: 0.3;
  }

  .glow-3 {
    width: 500px;
    height: 500px;
    background: radial-gradient(circle, #8b00ff 0%, transparent 70%);
    bottom: -20%;
    left: 30%;
    animation-duration: 6s;
    animation-delay: -2s;
    opacity: 0.2;
  }

  .glow-4 {
    width: 350px;
    height: 350px;
    background: radial-gradient(circle, #4f46e5 0%, transparent 70%);
    top: 30%;
    right: 20%;
    animation-duration: 4.5s;
    animation-delay: -0.5s;
    opacity: 0.25;
  }

  /* Grid cyberpunk */
  .cyber-grid {
    background-image: linear-gradient(
        rgba(0, 255, 255, 0.1) 1px,
        transparent 1px
      ),
      linear-gradient(90deg, rgba(0, 255, 255, 0.1) 1px, transparent 1px);
    background-size: 80px 80px;
    opacity: 0.3;
  }

  /* Posiciones de líneas de código */
  .line-1 {
    top: 20vh;
    left: 10vw;
    animation-delay: 0s;
  }
  .line-2 {
    top: 50vh;
    right: 15vw;
    animation-delay: -3s;
  }
  .line-3 {
    bottom: 30vh;
    left: 20vw;
    animation-delay: -6s;
  }

  /* Text shadows para efectos de neón */
  .code-text-glow-cyan {
    text-shadow:
      0 0 5px #00ffff,
      0 0 10px #00ffff,
      0 0 15px #00ffff;
  }
  .code-text-glow-magenta {
    text-shadow:
      0 0 5px #ff00ff,
      0 0 10px #ff00ff,
      0 0 15px #ff00ff;
  }
  .code-text-glow-indigo {
    text-shadow:
      0 0 5px #4f46e5,
      0 0 10px #4f46e5,
      0 0 15px #4f46e5;
  }

  /* Box shadows para partículas */
  .particle-shadow-cyan {
    box-shadow:
      0 0 6px #00ffff,
      0 0 12px #00ffff;
  }
  .particle-shadow-magenta {
    box-shadow:
      0 0 6px #ff00ff,
      0 0 12px #ff00ff;
  }
  .particle-shadow-indigo {
    box-shadow:
      0 0 6px #4f46e5,
      0 0 12px #4f46e5;
  }
  .particle-shadow-purple {
    box-shadow:
      0 0 6px #8b00ff,
      0 0 12px #8b00ff;
  }
  .particle-shadow-orange {
    box-shadow:
      0 0 6px #ff6600,
      0 0 12px #ff6600;
  }

  /* Posiciones de partículas */
  .particle-1 {
    top: 15%;
    left: 25%;
    animation-delay: 0s;
  }
  .particle-2 {
    top: 45%;
    left: 70%;
    animation-delay: -2s;
  }
  .particle-3 {
    top: 75%;
    left: 40%;
    animation-delay: -4s;
  }
  .particle-4 {
    top: 35%;
    left: 85%;
    animation-delay: -1s;
  }
  .particle-5 {
    top: 65%;
    left: 15%;
    animation-delay: -3s;
  }
  .particle-6 {
    top: 25%;
    left: 60%;
    animation-delay: -5s;
  }

  /* Responsive - Mobile First */
  @media (max-width: 767px) {
    .glow-1,
    .glow-2,
    .glow-3,
    .glow-4 {
      opacity: 0.2;
      filter: blur(20px);
    }
    .line-1,
    .line-2,
    .line-3 {
      display: none;
    }
    .cyber-grid {
      opacity: 0.1;
      background-size: 40px 40px;
    }
    .particle-1,
    .particle-2,
    .particle-3,
    .particle-4,
    .particle-5,
    .particle-6 {
      display: none;
    }
  }

  /* Tablet */
  @media (min-width: 768px) and (max-width: 1023px) {
    .glow-1,
    .glow-2,
    .glow-3,
    .glow-4 {
      opacity: 0.3;
    }
    .cyber-grid {
      opacity: 0.15;
      background-size: 60px 60px;
    }
  }

  /* Desktop */
  @media (min-width: 1024px) {
    .glow-1,
    .glow-2,
    .glow-3,
    .glow-4 {
      opacity: 0.4;
    }
    .cyber-grid {
      opacity: 0.3;
      background-size: 80px 80px;
    }
  }
</style>

<script>
  document.addEventListener("DOMContentLoaded", function () {
    const parallaxBack = document.querySelector(".parallax-back");
    const parallaxMid = document.querySelector(".parallax-mid");
    const parallaxFront = document.querySelector(".parallax-front");

    // Reset scroll position on page load to prevent background displacement
    function initializeParallax() {
      // Force initial parallax calculation to ensure proper positioning
      requestAnimationFrame(() => {
        updateParallax();
        // Double-check after a brief delay to ensure DOM is fully ready
        setTimeout(updateParallax, 100);
      });
    }

    function updateParallax() {
      const scrolled = window.scrollY;
      const rate1 = scrolled * -0.3; // Capa trasera más lenta
      const rate2 = scrolled * -0.5; // Capa media
      const rate3 = scrolled * -0.7; // Capa delantera más rápida

      if (parallaxBack && parallaxBack instanceof HTMLElement) {
        parallaxBack.style.transform = `translateY(${rate1}px) translateZ(-3px) scale(1.3)`;
      }
      if (parallaxMid && parallaxMid instanceof HTMLElement) {
        parallaxMid.style.transform = `translateY(${rate2}px) translateZ(-2px) scale(1.2)`;
      }
      if (parallaxFront && parallaxFront instanceof HTMLElement) {
        parallaxFront.style.transform = `translateY(${rate3}px) translateZ(-1px) scale(1.1)`;
      }
    }

    // Initialize parallax properly on page load
    initializeParallax();

    // Optimización con requestAnimationFrame
    let ticking = false;
    function requestTick() {
      if (!ticking) {
        requestAnimationFrame(() => {
          updateParallax();
        });
        ticking = true;
        setTimeout(() => {
          ticking = false;
        }, 16);
      }
    }

    window.addEventListener("scroll", requestTick);
  });
</script>
